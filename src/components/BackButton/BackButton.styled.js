import styled from 'styled-components';

export const StyledBackButton = styled.button`
  padding: 8px;
  position: absolute;
  top: 40px;
  left: 40px;
  display: flex;
  align-items: center;
  gap: 4px;
  background: none;
`;
